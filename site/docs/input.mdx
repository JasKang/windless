---
layout: asdf
---

# Input

import { useEffect, useState } from 'react'
import { Demo } from '../components/Demo'
import Input from '@/Input'
import LoadingIcon from '@/Icon/LoadingIcon'

export function InputDemo() {
  const [value, setValue] = useState()
  return (
    <div>
      <div className="mb-4">value: {value}</div>
      <Input value={value} onChange={setValue} />
    </div>
  )
}

<Demo>
  <InputDemo />
</Demo>

<div className="my-8 mx-auto flex flex-col gap-8 rounded bg-white p-8 ring-1 shadow ring-slate-100">
  <Input className="shadow" status="success" />
  <Input status="warning" prefix={<LoadingIcon />} />
  <Input status="warning" suffix={<LoadingIcon />} />
  <Input status="danger" />
</div>
